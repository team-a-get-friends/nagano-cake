<div class="container">
  <h2 class="mx-3 my-5 text-secondary border border-3 rounded-top col-2 text-center">商品詳細</h2>

  <div class="row">
    <div class="col-4">
      <%= image_tag @item.get_item_image(200,200) %>
    </div>
    <div class="col-8">
      <table>
        <tr>
          <td>商品名</td>
          <td><%= @item.name %></td>
        </tr>
        <tr>
          <td>商品説明</td>
          <td><%= @item.item_detail %></td>
        </tr>
        <tr>
          <td>ジャンル</td>
          <td><%= @item.genre.name %></td>
        </tr>
        <tr>
          <td>税込価格<br>(税抜価格)</td>
          <td><%= @item.with_tax_price %>(<%= @item.price %>)円</td>
        </tr>
        <tr>
          <td>販売ステータス</td>
          <td>
            <!--文字色をそれぞれ反映させるために修正→三項演算子で表現したい、、、-->
            <!--%= @item.is_active ? '販売中':'販売停止中' %>-->
            <% if @item.is_active %>
              <p class="fw-bold text-success">販売中</p>
            <% else %>
              <p class="fw-bold text-secondary">販売停止中</p>
            <% end %>
          </td>
        </tr>

      </table>

      <%= link_to "編集する", edit_admin_item_path,class:"btn btn-outline-success ml-2 btn-sm" %>
    </div>
  </div>
</div>
