<h4 class="mx-3 my-5 text-secondary border border-3 rounded-top col-2 text-center" alt="...">配送先登録/一覧</h4>

<!--, locals: {view_name: 'index'}-->
<%= render 'form', address: @address %>

<table class="table table-hover align-middle text-secondary">
  <thead>
    <tr>
      <th>郵便番号</th>
      <th>住所</th>
      <th>宛名</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @addresses.each do |address| %>
    <tr>
      <td>
        <%= address.post_code %>
      </td>
      <td>
        <%= address.address %>
      </td>
      <td>
        <%= address.name %>
      </td>
      <td>
        <%= link_to "編集する", edit_address_path(address.id), class:"btn btn-outline-success ml-3 btn-sm" %>
        <%= link_to "削除する", address_path(address.id), method: :delete, class:"btn btn-outline-danger ml-3 btn-sm" %>
      </td>
    <% end %>
    </tr>
  </tbody>
</table>